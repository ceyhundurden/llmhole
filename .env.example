# LLMHole configuration. Copy this file to `.env` and adjust as needed:
#   cp .env.example .env
#
# Live Mode talks to a local Ollama server, so there are no API keys anywhere -
# nothing in this file is a credential.

# Host port to publish the lab on.
LLMHOLE_PORT=8000

# HMAC key used to sign challenge flags. Change this if you host a CTF so flags
# are unique to your instance.
LLMHOLE_FLAG_SECRET=change-me-for-ctf-hosting

# Set to 0 to hide the built-in reference solutions.
LLMHOLE_ALLOW_SOLUTIONS=1

# Live Mode: where the browser-side lab reaches your Ollama server.
# - Running the lab in Docker with Ollama on your host: keep the compose default
#   (http://host.docker.internal:11434).
# - Running the lab with bare `uvicorn`: use http://localhost:11434.
# LLMHOLE_OLLAMA_ENDPOINT=http://host.docker.internal:11434

# --- Security / hardening (see SECURITY.md) ---
# Interface the container publishes on. Default 127.0.0.1 (loopback only).
# LLMHOLE_BIND=127.0.0.1
# CTF mode: fail fast on the default flag secret and hide reference solutions.
# LLMHOLE_CTF_MODE=0
# SSRF allow-list for Live Mode outbound calls (comma-separated hosts).
# LLMHOLE_ALLOWED_LLM_HOSTS=localhost,127.0.0.1,::1,host.docker.internal
# LLMHOLE_ALLOWED_LLM_PORTS=11434
# Session memory bounds.
# LLMHOLE_MAX_SESSIONS=5000
# LLMHOLE_SESSION_TTL=21600
# LLMHOLE_MAX_BUCKET_ITEMS=200
# Live Mode attempts allowed per session (runaway guard for your own hardware).
# LLMHOLE_MAX_LIVE_REQUESTS=100
